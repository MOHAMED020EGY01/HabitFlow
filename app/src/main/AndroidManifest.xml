<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name="com.example.app.HabitApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:localeConfig="@xml/locales_config"
        android:requestLegacyExternalStorage="true">

        <meta-data
            android:name="autoStoreLocales"
            android:value="true" />
        <activity
            android:name="com.example.app.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.example.core.infrastructure.receiver.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Overlay Foreground Service -->
        <service
            android:name="com.example.core.infrastructure.overlay.HabitOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:node="merge">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="habit_reminder_overlay" />
        </service>

        <!-- Background Reliability Foreground Service -->
        <service
            android:name="com.example.core.infrastructure.service.HabitBackgroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="habit_background_keepalive" />
        </service>

        <!-- Broadcast Receiver -->
        <receiver
            android:name="com.example.core.infrastructure.overlay.HabitOverlayReceiver"
            android:exported="false" />

        <!-- Pending Overlay Receiver (User Present/Unlock) -->
        <receiver
            android:name="com.example.core.infrastructure.overlay.PendingOverlayReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- All Habits Widget Receiver -->
        <receiver
            android:name="com.example.core.infrastructure.widget.AllHabitsWidgetReceiver"
            android:exported="true"
            android:label="HabitFlow — All Habits">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/all_habits_widget_info" />
        </receiver>

        <!-- Inactive Habits Widget Receiver -->
        <receiver
            android:name="com.example.core.infrastructure.widget.InactiveHabitsWidgetReceiver"
            android:exported="true"
            android:label="HabitFlow — Inactive Habits">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/inactive_habits_widget_info" />
        </receiver>

        <!-- Habit Summary Widget Receiver -->
        <receiver
            android:name="com.example.core.infrastructure.widget.HabitStatsSummaryWidgetReceiver"
            android:exported="true"
            android:label="HabitFlow — Habit Summary">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habit_stats_summary_widget_info" />
        </receiver>

    </application>

</manifest>
